<%= semantic_form_for [:admin, @aluno], :html => { :multipart => true }  do |f| %>  
	<%= f.inputs "Incluir primeira fase" do %>        
  		<%= f.input :txtFile1fase, :as => :file, :label => "Arquivo" %>    	
    <% end %>
  	<%= f.buttons %>
<% end %>

<%= semantic_form_for [:admin, @aluno], :html => { :multipart => true }  do |f| %>  
	<%= f.inputs "Incluir segunda fase" do %>        
  		<%= f.input :txtFile2fase, :as => :file, :label => "Arquivo" %>    	
    <% end %>
  	<%= f.buttons %>
<% end %>

<%= semantic_form_for [:admin, @aluno], :html => { :multipart => true }  do |f| %>  
	<%= f.inputs "Eliminar zeros" do %>
    <% end %>
  	<%= f.buttons %>
<% end %>
