<%= semantic_form_for [:admin, @aluno], :html => { :multipart => true }  do |f| %>  
	<%= f.inputs "Incluir primeira fase" do %>
        <%= f.input :ano %>
  		<%= f.input :txtFile, :as => :file, :label => "Arquivo" %>    	
    <% end %>
  	<%= f.buttons %>
<% end %>
